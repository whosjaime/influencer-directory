name: Daily Sponsor Lead Scan

on:
  schedule:
    - cron: "0 14 * * *"
  workflow_dispatch:
    inputs:
      target_daily_leads:
        description: Qualified NEW sponsor companies to aim for
        required: true
        default: "20"
      min_lead_score:
        description: Minimum lead quality score
        required: true
        default: "70"

# Never allow two sponsor scanners to write to monday at the same time.
concurrency:
  group: sponsor-lead-scanner
  cancel-in-progress: false

jobs:
  scan-sponsors:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate sponsor scanner
        run: |
          python -m compileall -q src
          python -m unittest discover -s tests -p 'test_sponsor_*.py'

      - name: Scan public sponsorships and import NEW leads
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          SPONSOR_MONDAY_TOKEN: ${{ secrets.SPONSOR_MONDAY_TOKEN }}
          SPONSOR_MONDAY_API_KEY: ${{ secrets.SPONSOR_MONDAY_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          SPONSOR_MONDAY_BOARD_ID: ${{ vars.SPONSOR_MONDAY_BOARD_ID }}
          SPONSOR_MONDAY_GROUP_ID: ${{ vars.SPONSOR_MONDAY_GROUP_ID }}
          SPONSOR_TARGET_DAILY_LEADS: ${{ github.event.inputs.target_daily_leads || vars.SPONSOR_TARGET_DAILY_LEADS || '20' }}
          SPONSOR_MIN_LEAD_SCORE: ${{ github.event.inputs.min_lead_score || vars.SPONSOR_MIN_LEAD_SCORE || '70' }}
          SPONSOR_SEARCH_REGION: ${{ vars.SPONSOR_SEARCH_REGION || 'US' }}
          SPONSOR_SEARCH_LANGUAGE: ${{ vars.SPONSOR_SEARCH_LANGUAGE || 'en' }}
          ENABLE_INSTAGRAM_SPONSOR_SCAN: ${{ vars.ENABLE_INSTAGRAM_SPONSOR_SCAN || 'false' }}
          ENABLE_TIKTOK_SPONSOR_SCAN: ${{ vars.ENABLE_TIKTOK_SPONSOR_SCAN || 'false' }}
        run: python src/run_sponsor_scan.py
